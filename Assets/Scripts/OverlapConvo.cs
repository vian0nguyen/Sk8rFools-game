using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OverlapConvo : MonoBehaviour
{

    //add triggers to convo canvas
    //if they are in the zone (stay)
    //move their position until they are not anymore
    // Start is called before the first frame update

        //INSTEAD!! use similar code to the q not walking ontop of clay code
        //if dialog bubble of active player is __
        //make that bubble a higher sorting layer
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
